apiVersion: v1
kind: Service
metadata:
  name: predictive-maintenance
spec:
  selector:
    app: predictive-maintenance
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
